import { Theme, createStyles } from '@material-ui/core/styles';

export const styles = (theme: Theme) =>
   createStyles({
      root: {
         cursor: 'pointer',
      },
      card: {
         width: '100%',
      },
   });
