query getMovie($uuid: String!) {
   getMovie(uuid: $uuid) {
      freeServer1
      freeServer2
      vipServer1
      vipServer2
      name
      release_date 
      body
      genres {
         name
      }
   }
}
